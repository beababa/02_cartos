---
title: "Antenne relais"
author: "B. Maranget"
date: "18/12/2024"
output: 
  html_document: 
    number_sections: yes
    toc: yes
editor_options: 
  chunk_output_type: console
---


```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
knitr::opts_chunk$set(cache = TRUE)
# Passer la valeur suivante à TRUE pour reproduire les extractions.
knitr::opts_chunk$set(eval = FALSE)
knitr::opts_chunk$set(warning = FALSE)
```


# Objet

Récupération de la donnée opendata sur Bondy

https://data.anfr.fr/visualisation/table/?id=observatoire_2g_3g_4g&sort=adr_nm_cp&q=93140&location=11,48.98945,2.60719&dataChart=%3D

ANFR agence nationale des fréquences




# Environnement


## Librairies


```{r , eval=TRUE}
library(sf)
library(mapsf)
```


## Chemins


```{r}
chemin <- "C:/Users/bmaranget/Documents/03_SIG/03_03_Data/25_DEV_DURABLE/"
chemin2 <- "C:/Users/bmaranget/Documents/03_SIG/03_03_Data/03_SOCLE/"
```



# Données


```{r}
bondy <- st_read("../data/limitesSocle.gpkg", "bondy")
tampon <- st_buffer(bondy, 1000)
mf_map(tampon)
mf_map(bondy, add = T)
```



```{r}
# Toute la France
antenne <- st_read(paste0(chemin,"sourceANFR_France.geojson"))
antenne <- st_read("../data/observatoireod_20260521.geojson")
antenne <- st_transform(antenne, 2154)
mf_map(inter, add = T)
# Uniquement Bondy (tampon)
inter <- st_intersection(antenne, tampon)
inter
st_write(inter [, c("adm_lb_nom","emr_lb_systeme","emr_dt", "code_insee", "generation", "nat_id","sup_nm_haut",
                    "adr_lb_add1")], "../data/antenneRelais.gpkg", "mai2026")
antenne <- st_read(paste0(chemin,"antenneRelais.gpkg"))
```


```{r}
bat <- st_read(paste0(chemin2, "BDTOPO_3-3_TOUSTHEMES_SHP_LAMB93_D093_2024-03-15/BDTOPO/1_DONNEES_LIVRAISON_2024-04-00042/BDT_3-3_SHP_LAMB93_D093-ED2024-03-15/BATI/BATIMENT.shp"))
```

on utilise désormais happign

```{r}
library(happign)
wfs_layers <- get_layers_metadata(data_type = "wfs")
wfs_layers$Name [grep("^BDTOPO_V3:ba", wfs_layers$Name)]
bat <- get_wfs(
  x = tampon,
  layer = "BDTOPO_V3:batiment",
  predicate = within()
)
```

ouaouh / 45244

Intersections

```{r}
bat <- st_transform(bat, 2154)
batSel <- st_intersection(bat  [bat$hauteur > 15,], tampon)
mf_map(batSel)
batSel <- batSel [,c("hauteur")]
# 872 bat >15 m
# si pb gééométrie
batSel <- st_cast(bat, "MULTIPOLYGON")
# par défaut les immeubles sans hauteur ne sont pas importants
sanshauteur <- batSel [is.na(batSel$hauteur),]
batSel <- batSel [!is.na(batSel$hauteur)]
# par curiosité
batSel$aire <- st_area(batSel$geometry)
summary(batSel$aire)
hist(batSel$aire)
library(units)
batSel$aire <- drop_units(batSel$aire)
mf_map(batSel [batSel$aire > 600,])
mf_layout("gros batiment (aire)", "BDTOPO")
# recodage
batSel$taille <- NA
batSel$taille [batSel$hauteur  >= 15 & batSel$hauteur < 20] <- "petit"
batSel$taille [batSel$hauteur  >= 20 & batSel$hauteur < 40] <- "moyen"
batSel$taille [batSel$hauteur  >= 40 & batSel$hauteur < 60] <- "grand"
# fusion avec critère
aggBat <- aggregate(batSel [,c("taille")], by= list(batSel$taille), length )
names (aggBat) [1:2] <- c("taille","nb")
st_write(aggBat,"../data/antenneRelais.gpkg", "bat_tampon2026", delete_layer = T)
antenneSel <- st_intersection(antenne, tampon)
```


```{r}
mf_init(tampon)
mf_map(bondy, col="wheat", border = NA, add = T)
mf_map(antenneSel, add = T)
mf_typo(aggBat, var = "taille", border = NA, add = T)
mf_layout("Antennes relais et batiments > 15 m", "IGN, ANFR\nMai 2026")
```




# Agregation

## Par type de fréquence

```{r}
# intersection tampon antenne
antenne <- antenneSel
# filtre sur le 5 G
prop.table(table(antenne$generation))*100
table(antenne$generation)
antenne5G <- antenne [antenne$generation == '5G',]
antenne4G <- antenne [antenne$generation == '4G',]
```

Agrégation par adresse et fournisseur

```{r}
# filtre sur les 4 G
data <- antenne4G
# détermination du nb de points d'implantation quelque soit le fournisseur et la fréquence
agg <- aggregate(data [, c("adr_lb_add1","geometry")], by=list(data$adr_lb_add1), length)
# 77 pour le tampon
# 108 sur tampon 1 km
# founisseur
agg <- aggregate(data [, c("adr_lb_add1","geometry")], by=list(data$adr_lb_add1,  data$adm_lb_nom), length)
# 107
# 146
agg <- st_jitter(agg)
names(agg)[1:3]  <- c("Adresse", "Fournisseur", "nb")
hist(agg$nb)
# le nb varie de 5 à 10 avec les fréquences
st_write(agg, "../data/antenneRelais.gpkg", "aggFournisseur_tampon_4G_2026", delete_layer = T)
summary(agg$nb)
length(agg$nb)
sum(agg$nb)
# 439 antennes 4 G
# 703 tampon 1 km
```


## Par antenne

En vue de créer une carte de densité pondérée par le nb d'antennes sur chaque point.

```{r}
data <- antenneSel
agg <- aggregate(data [, c("adr_lb_add1","geometry")], by=list(data$adr_lb_add1), length)
names(agg)[1:2]  <- c("Adresse",  "nb")
hist(agg$nb)
```


# Densité


## Grille

Aire couverte par les antennes

```{r}
agg <- st_transform(agg, 2154)
enveloppe <- st_convex_hull(st_union(agg))
mf_map(enveloppe)
aire <- st_area(enveloppe)
aire/ 10000
# ha 1250 / 500 ha Bondy
```


```{r}
grid <- st_make_grid(x = agg, cellsize = 200)
grid <- st_sf(ID = 1:length(grid), geom = grid)
mf_map(grid)
mf_label(grid, var = "ID")
```

Intersection pt / grille

```{r}
inter <- st_intersection(agg, grid)
```

Agrégation par carreau


```{r}
length(inter$ID)
inter$Adresse
length(inter$nb)
agg <- aggregate(inter [ ,c("nb"), drop = T], by = list(inter$ID), sum)
names(agg) <- c("ID", "nb")
jointure <- merge(grid, agg, by= "ID")
mf_choro(jointure, var = "nb")
st_write(jointure, "../data/antenneRelais.gpkg", "densite_tampon_2026", delete_layer = T)
```


```{r}
mf_init(tampon)
mf_map(bondy, col = "wheat", border = NA)
mf_choro(jointure, var = "nb", border= NA, add = T)
mf_layout("densité antennes relais","ANFR, IGN\mai 2026")
summary(jointure$nb)
```


# En 2026, rajoute des zones urbaines



```{r}
get_gpu_layers()
zone <- get_apicarto_gpu(bondy,"zone-urba")
zone <- st_cast(zone, "POLYGON")
zone <- zone [st_is_valid(zone),]
zone <- st_transform(zone, 2154)
inter <- st_intersection (bondy, zone) 
mf_map(interSel, "typo", var = "libelong")
interSel <- inter [grep("^UR|^UA|^UEi", inter$libelle),]
table(inter$libelle)
table(interSel$libelle)
# zones PLUi UR UA et UEi 
```


```{r}
par(mfrow = c(1,3))
col <- c("wheat", "purple", "cadetblue2")
i <- 1
zone <- c("^UR","^UA","^UEi")
for (z in zone){
  print(z)
  tmp <- inter [grep(z, inter$libelle),]
  mf_map(tmp, col = col [i], border = NA)
  mf_layout(z, "GPU\nMai 2026")
  i <- i+ 1
}
```
  

```{r}
palBat <- c("green", "orange", "yellow")
png("../img/antennePtsansBat.png", height = 10000, width = 10000, res = 1000)

mf_map(bondy, col = "cornsilk")
mf_map(interSel, type = "typo", var = "libelong", pal = col, border = NA, add = T)
#mf_map(aggBat, type = "typo", var = "taille", pal = palBat, border = NA,add = T, leg_pos = "right")
mf_map(antenne, add = T, col="red")
mf_layout("Antennes relais, immeubles sup. 5 m et zones urbaines", "GPU,ANFR\nMai 2026")
dev.off()
```

```{r}
png("../img/antenneCarsansBat.png", height = 10000, width = 10000, res = 1000)

mf_map(bondy, col = "cornsilk")
mf_map(interSel, type = "typo", var = "libelong", pal = col, border = NA, add = T)
#mf_map(aggBat, type = "typo", var = "taille", pal = palBat, border = NA,add = T, leg_pos = "right")
mf_map(jointure, add = T, "choro", var = "nb", border = NA, alpha = 0.5)
mf_layout("Antennes relais, immeubles sup. 5 m et zones urbaines", "GPU,ANFR\nMai 2026")
dev.off()
```

